# Local-only Vite env (not committed). Native build points API calls here.
# Dev: your PC's LAN IP so the phone can reach the dev server over Wi-Fi.
# For a production app build, set this to the deployed API origin (https://...).
VITE_NATIVE_API_BASE=http://192.168.1.186:3001
